# ghostchat_bot

Анонімний чат у Telegram на **aiogram v3** з пошуком співрозмовника, пересиланням повідомлень, скаргами та адмін‑командами.

## Функції
- /start з головним меню
- Пошук співрозмовника (черга)
- Пошук за інтересами
- Анонімне пересилання тексту та медіа
- Завершення діалогу
- Профіль користувача
- Скарги та бан
- Адмін‑команди: /ban, /unban, /stats

## Структура
```
.
  src/
    main.py
    config.py
    db/
      database.py
      models.py
      queries.py
    bot/
      routers/
        start.py
        match.py
        chat.py
        profile.py
        reports.py
        admin.py
      keyboards/
        main_menu.py
        match_menu.py
        report_menu.py
      middlewares/
      utils/
        chat.py
        constants.py
        users.py
  .env.example
  requirements.txt
  README.md
```

## Як запустити
1) Створити та активувати venv
```
python -m venv .venv
source .venv/bin/activate
```

2) Встановити залежності
```
pip install -r requirements.txt
```

3) Налаштувати .env
```
cp .env.example .env
```
Заповнити `TOKEN`, `ADMIN_ID` (можна кілька через кому), за потреби `DB_PATH`.

4) Запуск
```
python -m src.main
```

## Адмін‑команди
- `/admin` — адмін‑панель (статистика, активні користувачі, бан/розбан)
- `/ban <user_id>` — заблокувати користувача
- `/unban <user_id>` — розблокувати користувача
- `/stats` — статистика (користувачі, активні чати, черга, скарги)

## Адмін‑переваги в чаті
- Кнопка **🧷 Адмін: інфо партнера** — показує ID та username партнера
- Кнопка **🚫 Адмін: бан партнера** — банить партнера та завершує діалог

## Інтереси
- Кнопка **🎯 Інтереси** — вибір теми для пошуку
- Пошук підбирає співрозмовника з таким самим інтересом
